 const view = new MapView({
    container: "viewDiv",
    map: map,
    center: [{{viewX}}, {{viewY}}], //Longitude, latitude
    zoom: 12,
});
const routeTask = new RouteTask({
    url: "https://route-api.arcgis.com/arcgis/rest/services/World/Route/NAServer/Route_World",
});
let graphic;
// ! STARTING POINT
graphic = new Graphic({
    symbol: {
    type: "simple-marker",
    color: "white",
    size: "8px",
    },
    geometry: {
    type: "point", // autocasts as new Point()
    longitude: {{startX}},
    latitude: {{startY}},
    },
});
view.graphics.add(graphic);

// ! END POINT
graphic = new Graphic({
    symbol: {
    type: "simple-marker",
    color: "black",
    size: "8px",
    },
    geometry: {
    type: "point", // autocasts as new Point()
    longitude: {{endX}},
    latitude: {{endY}},
    },
});
view.graphics.add(graphic);